-- =====================================
-- SQL PRACTICE PROGRAM
-- =====================================

-- 1. Create a database
CREATE DATABASE college_db;

-- 2. Select the database
USE college_db;

-- 3. Delete students table if it already exists
DROP TABLE IF EXISTS students;

-- 4. Create Students table
CREATE TABLE students(
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    phone VARCHAR(15),
    date_of_birth DATE,
    is_active BOOLEAN DEFAULT TRUE,
    marks DECIMAL(5,2) CHECK (marks >= 0 AND marks <= 100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 5. Display all tables
SHOW TABLES;

-- 6. Display structure of students table
DESCRIBE students;

-- 7. Insert student records
INSERT INTO students(name,email,phone,date_of_birth,marks)
VALUES
('Rahul','rahul@gmail.com','9876543210','2004-05-10',91.50);

INSERT INTO students(name,email,phone,date_of_birth,marks)
VALUES
('Priya','priya@gmail.com','9876543211','2003-08-15',88.25);

INSERT INTO students(name,email,phone,date_of_birth,marks)
VALUES
('Arjun','arjun@gmail.com','9876543212','2002-12-05',76.75);

-- 8. Display all records
SELECT * FROM students;

-- 9. Display only names and marks
SELECT name, marks
FROM students;

-- 10. Update marks
UPDATE students
SET marks = 95.50
WHERE id = 1;

-- 11. Display updated table
SELECT * FROM students;

-- 12. Delete one student
DELETE FROM students
WHERE id = 3;

-- 13. Display remaining students
SELECT * FROM students;

-- 14. Create another table
CREATE TABLE tasks(
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    description TEXT,
    priority ENUM('LOW','MEDIUM','HIGH') DEFAULT 'MEDIUM',
    status ENUM('PENDING','IN_PROGRESS','COMPLETED','CANCELLED') DEFAULT 'PENDING',
    due_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    is_archived BOOLEAN DEFAULT FALSE,
    assignee VARCHAR(100),
    estimated_hours DECIMAL(5,2),
    actual_hours DECIMAL(5,2)
);

-- 15. Display all tables
SHOW TABLES;

-- 16. Show tasks structure
DESCRIBE tasks;

-- 17. Insert task
INSERT INTO tasks(title,description,priority,assignee,estimated_hours)
VALUES(
'Complete SQL Assignment',
'Practice all SQL commands',
'HIGH',
'Rahul',
4.50
);

-- 18. View tasks
SELECT * FROM tasks;

-- 19. Delete tasks table
DROP TABLE IF EXISTS tasks;

-- 20. Delete students table
DROP TABLE IF EXISTS students;



create database users;
use users;
CREATE TABLE user(
id INT,
Name VARCHAR(10),
email VARCHAR(20),
cgpa DECIMAL(2,1)
);

INSERT INTO user VALUES (1,"AHWIN","ashwin@gmail.com",7.8);
SELECT * FROM user;


USE  users;
CREATE TABLE orders(
id INT,
status varchar(20) DEFAULT 'PENDING',
created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
INSERT INTO orders(id,status)
VALUES(2,'COMPLETED');
SELECT * FROM orders


use users;
CREATE TABLE students(
id INT,
COURSE_ID INT,
PRIMARY KEY (id, course_id)
);

INSERT INTO students (id, COURSE_ID) values (1,1);
INSERT INTO students (id, COURSE_ID) values (1,3);
INSERT INTO students (id, COURSE_ID) values (1,NULL);
SELECT * FROM students;



use users;
CREATE TABLE students2(
id INT unique,
PRIMARY KEY (id)
);

INSERT INTO students (id) values (1);
SELECT * FROM students;




